# Story 1.5: Verify `SESSION_SECRET` in Vercel; ops doc

## Status

Done

> **Gate CONCERNS (@architect, 2026-09-14) — `docs/qa/gates/1.5-verify-session-secret-vercel.yml`.**
> The story was Done for its agent-executable scope (AC3/AC4/AC5/AC6). **AC1/AC2
> resolved 2026-09-14** — see the update below and QA-1.5-01's resolution.

> **AC1/AC2 resolved 2026-09-14 (with the user's live permission).** `vercel` CLI
> installed, `vercel login`/`vercel link` completed interactively by the user,
> repo linked to `leticia-lehn/wiz-timetable`. `vercel env ls` showed
> `SESSION_SECRET` **absent from every environment** (Production, Preview,
> Development) — not weak, entirely missing. Generated a fresh
> `openssl rand -base64 32` value (never printed, piped directly via stdin) and
> added it to Production and Preview via `vercel env add`, stored as Vercel's
> `Secret` type (hidden even from `vercel env pull`). Confirmed present in both via
> `vercel env ls`. This was an **add, not a rotation** — no prior value existed, so
> no active session/user was logged out. Epic AC8 is now satisfied.

## Executor Assignment

executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [manual-verification, doc-review]

## Story

**As a** deployment owner,
**I want** `SESSION_SECRET` confirmed set and cryptographically strong in the Vercel
production environment, and that fact documented,
**so that** the session cookie that gates every route and server function
(Stories 1.1–1.3) cannot be forged or brute-forced, and the next person touching
deploys knows this variable exists and why it matters.

## Acceptance Criteria

1. `SESSION_SECRET` is confirmed present in Vercel's **Production** environment
   variables for this project (via `vercel env ls production` or the Vercel
   dashboard) — not just in local `.env`.
2. The value is confirmed strong: at minimum 32 bytes of high-entropy randomness
   (e.g. `openssl rand -base64 32` or equivalent), not a short/guessable/default
   string. If the current value does not meet this bar, generate a new one and
   update it in Vercel (Production **and** Preview, so preview deployments do not
   silently share or fall back to a weak secret) — this **will invalidate all
   existing sessions**, forcing every logged-in user to log in again; call this out
   explicitly when rotating.
3. `SESSION_SECRET` is added to `.env.example` (currently missing — confirmed by
   inspection; only `SUPABASE_*` / `VITE_SUPABASE_*` keys are listed there today) as
   a documented placeholder, e.g. `SESSION_SECRET=` with a comment pointing at
   `auth.server.ts`'s `sessionConfig()`.
4. `docs/framework/tech-stack.md` documents `SESSION_SECRET`: what it is
   (password for the `escola-auth` `useSession` cookie, PBKDF2-backed encryption via
   `@tanstack/react-start/server`), where it must be set (Vercel Production +
   Preview env vars, plus local `.env`), what happens if it's missing (`auth.server.ts`
   throws `"SESSION_SECRET não configurado"`; per Story 1.1's fix, `__root.tsx`'s
   `beforeLoad` catches this and falls back to showing the login screen rather than a
   500 — but every request still fails auth until the variable is set), and the
   strength requirement from AC2. This satisfies epic AC8 (`SESSION_SECRET`
   confirmed set and strong in Vercel; documented in
   `docs/framework/tech-stack.md`) — the file already exists (created during
   brownfield discovery), so this story adds a section to it rather than creating it
   from scratch. **Corrected by @po 2026-09-14:** `tech-stack.md` contains **zero**
   occurrences of `SESSION_SECRET` today (verified by grep); the "Runtime targets"
   env-var row that lists it is in `docs/architecture.md` (line 53), a different file.
   Treat AC4 as _adding the first mention of `SESSION_SECRET` to `tech-stack.md`_,
   not as expanding an existing one. While there, add `SESSION_SECRET` to
   `tech-stack.md`'s "Runtime targets" env context too, so the two docs agree.
5. The verification steps and result (present/absent, rotated or not, date checked)
   are recorded in this story's Completion Notes and in `docs/estado.md`'s
   EPIC-001 table, so the check is auditable without re-running it.
6. No application code changes in this story — it is a config-verification +
   documentation story only.

## 🤖 CodeRabbit Integration

**Not applicable** — no source code diff expected (env var check + two doc files).
Complexity: **Low (T-shirt: S)** — no application code, no schema; the only real risk
is the session-invalidation side effect of a rotation (AC2). _(Estimate added by @po
during validation; the draft omitted it.)_
If the rotation in AC2 is needed, no code changes are still required (the secret is
read from `process.env.SESSION_SECRET` at runtime; nothing to redeploy code for).

## Tasks / Subtasks

- [x] Task 1: Verify presence in Vercel (AC: 1) — **DONE 2026-09-14**
  - [x] `vercel env ls` (no filter) — `SESSION_SECRET` was absent everywhere;
        added via `vercel env add`, now confirmed listed for Production.
  - [x] Also checked Preview — added and confirmed present there too.
- [x] Task 2: Verify/rotate strength (AC: 2) — **DONE 2026-09-14 (add, not rotate)**
  - [x] `vercel env ls` showed no existing value to pull/assess — nothing to
        inspect, nothing to rotate.
  - [x] Generated a fresh value (`openssl rand -base64 32`, piped via stdin, never
        printed) and set it via `vercel env add SESSION_SECRET production` and
        `... preview`. No `vercel env rm` needed — there was no old value.
  - [x] Not a rotation: no session was invalidated, since no `SESSION_SECRET` was
        ever live on Vercel for any deployed code to have used. Noted in
        Completion Notes.
- [x] Task 3: Update `.env.example` (AC: 3)
  - [x] Add `SESSION_SECRET=` with a one-line comment.
- [x] Task 4: Document in tech-stack.md (AC: 4)
  - [x] Add a short subsection (e.g. under "Runtime targets" or a new "Secrets"
        section) covering the 4 points in AC4: what it protects, where it lives,
        failure mode, strength requirement.
- [x] Task 5: Record the audit trail (AC: 5)
  - [x] Update this story's Completion Notes with: date checked, present/absent
        result, rotated yes/no, who/what tool was used to check.
  - [x] Update `docs/estado.md`'s EPIC-001 table row for 1.5.

## Completion Notes (@devops / Gage — 2026-09-14)

### Done

- **AC3 — `.env.example`.** `SESSION_SECRET=""` added at the top of the file with a
  4-line comment pointing at `sessionConfig()` in `src/lib/auth.server.ts`, the
  `openssl rand -base64 32` strength bar, the Vercel Production + Preview
  requirement, and the new tech-stack.md section. Placeholder is empty — no real
  value committed.
- **AC4 — `docs/framework/tech-stack.md`.** Added a new `## Secrets` section with a
  `### SESSION_SECRET` subsection covering all four required points (what it
  protects and why forging it bypasses Stories 1.1–1.3; where it must be set, as a
  table for Production / Preview / local; the `>= 32 bytes` strength bar plus the
  session-invalidation cost of rotating; and the missing-value failure mode —
  `"SESSION_SECRET não configurado"` caught by `__root.tsx`'s `beforeLoad`, so the
  symptom is a login screen nobody can get past, not a 500). Also added an
  "Env vars (server)" row to the **Runtime targets** table listing
  `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_PUBLISHABLE_KEY`,
  `SESSION_SECRET`, so tech-stack.md and `docs/architecture.md:53` now agree.
- **AC6 — no application code changes.** Confirmed: only `.env.example`,
  `docs/framework/tech-stack.md`, `docs/estado.md`, and this story file were touched.

### Resolved 2026-09-14 — see below for the original blocker record

**AC1/AC2 are now DONE.** `SESSION_SECRET` did not exist in any Vercel environment
(checked: Production, Preview, Development — all absent, confirmed via
`vercel env ls` with no environment filter). This was an **add**, not a
**rotation**: `openssl rand -base64 32` generated locally, piped directly into
`vercel env add SESSION_SECRET production` / `... preview` via stdin (never
printed to any log, terminal history, or file), stored as Vercel's `Secret` type
(hidden even from `vercel env pull` — stronger than this story originally assumed
possible). Verified present in both environments via `vercel env ls`. Because no
value previously existed, **no active user session was invalidated** — the
"announce before rotating" caution below did not apply.

<details>
<summary>Original blocker record (2026-09-14, before human action)</summary>

**AC1** (present in Vercel Production + Preview) and **AC2** (value is >= 32 bytes of
high-entropy randomness, rotate if not) could **not** be executed here:

- `vercel` is not on PATH and is not a project dependency
  (`npx --no-install vercel` → package absent; no `node_modules/.bin/vercel`).
- The repo is **not linked** to a Vercel project: `.vercel/` contains only
  `output/` (a build artifact); there is no `.vercel/project.json`.
- Logging in requires an interactive browser/device flow, which this agent
  environment cannot perform. Per the @po's note, this is a prerequisite, not a
  story failure.

**Commands for the human (one copy-paste pass, from the repo root):**

```bash
npm i -g vercel          # or: npx vercel@latest <subcommand>
vercel login             # interactive — opens a browser
vercel link              # select the Wiz Timetable project; creates .vercel/project.json

vercel env ls production # AC1: confirm SESSION_SECRET is listed
vercel env ls preview    # AC1: same for Preview

# AC2 — inspect strength WITHOUT printing the secret:
vercel env pull .env.vercel-check --environment=production
node -e "const l=require('fs').readFileSync('.env.vercel-check','utf8').split(/\r?\n/).find(x=>x.startsWith('SESSION_SECRET='));const v=l.split('=').slice(1).join('=').trim().replace(/^\"|\"$/g,'');console.log({len:v.length,unique:new Set(v).size,looksBase64:/^[A-Za-z0-9+/=]+$/.test(v)})"
rm .env.vercel-check     # do NOT commit this file
```

**What to check for:**

- AC1 passes if `SESSION_SECRET` appears in **both** `env ls production` and
  `env ls preview`.
- AC2 passes if the pulled value looks like `openssl rand -base64 32` output:
  `len` ~44 (or >= 43), `unique` roughly 30–40, `looksBase64: true`. A low `unique`
  count relative to `len`, or `looksBase64: false` with dashes/words, means it is a
  human passphrase → **rotate**.
- If rotating: `vercel env rm SESSION_SECRET production` then
  `vercel env add SESSION_SECRET production` (paste `openssl rand -base64 32`
  output), and repeat for `preview`. **This logs out every active user** — announce
  it first. No redeploy of application code is required.

</details>

### Signal worth acting on

Without touching Vercel, the **local** `.env.local` was inspected for shape only
(value never printed, never committed): `SESSION_SECRET` is present, 52 characters,
but only **17 distinct characters**, all lowercase letters plus separators — no
digits, no uppercase, not base64 and not hex. That is the fingerprint of a
human-readable passphrase, **not** 32 bytes of randomness. `.env` (as opposed to
`.env.local`) has no `SESSION_SECRET` line at all.

**Resolution note:** this local value turned out to be unrelated to Vercel — Vercel
had no `SESSION_SECRET` at all (see "Resolved 2026-09-14" above), so there was
nothing on Vercel to inherit the local passphrase's weakness. The weak local value
in `.env.local` is out of this story's scope (local dev only, gitignored,
never touched) but worth a maintainer's attention separately if local dev security
matters here.

If Vercel was seeded from the same source as the local file, **AC2 will very likely
fail and a rotation will be required.** Plan for the forced-logout side effect.

### Audit trail

| Item                         | Result                                                                   |
| ---------------------------- | ------------------------------------------------------------------------ |
| Date checked                 | 2026-09-14                                                               |
| Checked by                   | @devops (Gage), automated agent session                                  |
| Tool used                    | local shell; `vercel` CLI **unavailable**                                |
| Vercel Production — present? | **Not verified** (CLI unavailable / project unlinked)                    |
| Vercel Preview — present?    | **Not verified** (same)                                                  |
| Rotated?                     | **No** — rotation not attempted, requires Vercel access + owner sign-off |
| Local `.env.local`           | Present; shape suggests a passphrase, not 32 random bytes                |

## Dev Notes

- **Ground truth for what this variable does**, from `src/lib/auth.server.ts`:
  ```ts
  function sessionConfig() {
    const password = process.env.SESSION_SECRET;
    if (!password) throw new Error("SESSION_SECRET não configurado");
    return {
      password,
      name: "escola-auth",
      maxAge: 60 * 60 * 24 * 7,
      cookie: { httpOnly: true, secure: true, sameSite: "lax", path: "/" },
    };
  }
  ```
  This is the encryption password for `@tanstack/react-start/server`'s
  `useSession()` — every request's `escola-auth` cookie is encrypted/decrypted with
  it. A weak or leaked secret lets an attacker forge a valid session cookie for any
  `usuarioId`, bypassing every guard added in Stories 1.1–1.3.
- **Fail-closed behavior already shipped** (Story 1.1, `src/routes/__root.tsx`):
  if `getSessaoAtual()` throws (which it will if `SESSION_SECRET` is unset), the root
  `beforeLoad` catches it and renders the login screen instead of crashing the whole
  app with a 500. This story is about the secret being _set and strong_, not about
  the missing-secret failure mode, which is already handled defensively.
- **`docs/framework/tech-stack.md` already exists** — it is the file referenced by
  epic AC8, created during the brownfield discovery documented in `docs/estado.md`.
  **Correction (@po, 2026-09-14):** the draft claimed this file "already references
  `SESSION_SECRET` once in passing (Runtime targets table)". That is **not accurate** —
  `grep -n "SESSION_SECRET" docs/framework/tech-stack.md` returns nothing. The env-var
  row that lists `SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_PUBLISHABLE_KEY,
SESSION_SECRET` lives in `docs/architecture.md:53`. `tech-stack.md`'s own "Runtime
  targets" table has no env-vars row at all. So AC4 introduces the first mention of
  this variable in `tech-stack.md`. (`tech-stack.md` _is_ otherwise consistent with
  this story: its "Not in the stack" section already states "**No Supabase Auth** —
  auth is hand-rolled (`usuarios` table + session cookie)".)
- **`.env.example` gap confirmed by inspection**: it lists `SUPABASE_PROJECT_ID`,
  `SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, and their
  `VITE_*` mirrors, plus a block of unrelated AIOX tooling keys — no
  `SESSION_SECRET` line at all today.
- **Do not commit** the real secret value anywhere (story file, `.env.example`,
  git history). `.env.example` gets a blank placeholder only; `.env`/`.env.local` are
  already git-ignored per `docs/framework/tech-stack.md`.
- **This is a @devops-executed story** per the epic's delegation table — no
  application code review is expected; @architect's quality gate here is about
  confirming the verification was actually performed and documented, not code
  review.

### Testing

- No automated test applies (infra/config verification). Evidence = the completed
  Task 1/2 checklist + the `docs/estado.md` and `tech-stack.md` diffs.

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Author            |
| ---------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| 2026-09-14 | 0.1     | Initial draft from EPIC-001 AC8, grounded by reading `auth.server.ts`, `__root.tsx`, `docs/framework/tech-stack.md`, and `.env.example`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | @sm (River)       |
| 2026-09-14 | 0.2     | `*validate-story-draft` GO (8/10) — Draft → Ready. Corrected an inaccurate grounding claim about `tech-stack.md` (AC4 + Dev Notes) and added the missing complexity estimate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | @po (Pax)         |
| 2026-09-14 | 0.3     | Ready → InProgress → InReview. AC3/AC4/AC6 implemented (`.env.example`, `tech-stack.md` § Secrets + Runtime targets env row). AC1/AC2 blocked: Vercel CLI unavailable and project unlinked — exact verification commands recorded in Completion Notes.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | @devops (Gage)    |
| 2026-09-14 | 0.4     | `*qa-gate` → **CONCERNS** — **InReview → Done**. Gate file: `docs/qa/gates/1.5-verify-session-secret-vercel.yml`. Diff re-read in full: AC3/AC4/AC5/AC6 verified (no secret committed; § Secrets matches `auth.server.ts`; zero files under `src/`). AC1/AC2 remain BLOCKED on human Vercel access — not a code defect, disclosed with exact remediation, so CONCERNS rather than FAIL (FAIL would return the story to an executor with no executable fix). WAIVED rejected: the work is deferred, not accepted-and-closed. **Epic AC8 is explicitly NOT closed by this Done.** QA-1.5-01 (high, security): the local secret's shape suggests a passphrase — measure and expect to rotate Production + Preview. | @architect (Aria) |

## File List

- `.env.example` (modified — added `SESSION_SECRET` placeholder + comment)
- `docs/framework/tech-stack.md` (modified — new `## Secrets` section, Runtime targets env-vars row)
- `docs/estado.md` (modified — EPIC-001 row for 1.5)
- `docs/stories/1.5.verify-session-secret-vercel.md` (this file)

### @po validation note (2026-09-14, 8/10 — GO)

Verified against source: `auth.server.ts:7-16` `sessionConfig()` matches the snippet
quoted in Dev Notes exactly; `__root.tsx:61-72` confirms the fail-closed `beforeLoad`
try/catch from Story 1.1; `.env.example` confirmed to have no `SESSION_SECRET` line.

Two deductions, both corrected in place rather than bounced back, since neither
changes the work to be done:

- **Point 10 (alignment) −1:** Dev Notes and AC4 asserted `tech-stack.md` already
  mentions `SESSION_SECRET`. It does not (0 matches). The claim conflated
  `tech-stack.md` with `docs/architecture.md:53`. Article IV concern — corrected above.
- **Point 6 (complexity) −1:** no estimate anywhere in the draft (the CodeRabbit
  section, which carries the estimate in Stories 1.1/1.3/1.4, said only "Not
  applicable"). Added as Low / T-shirt S.

Open item for the executor (@devops): AC1/AC2 assume the Vercel project is linked and
`vercel` CLI is authenticated for this repo. If it is not, that is a prerequisite,
not a story failure — record it and use the dashboard path already offered in Task 1.
| 2026-09-14 | 0.5 | **AC1/AC2 RESOLVED** with the user (permission granted live). `vercel` CLI installed, user completed the interactive `vercel login` (device flow) and I ran `vercel link` (repo linked to `leticia-lehn/wiz-timetable`). `vercel env ls` (no filter) confirmed `SESSION_SECRET` absent from Production, Preview, AND Development — not weak, entirely missing. Generated `openssl rand -base64 32` locally, piped directly into `vercel env add SESSION_SECRET production`/`preview` via stdin (never printed anywhere), stored as Vercel Secret type. Confirmed present in both via `vercel env ls`. No rotation occurred (no prior value existed) so no user was logged out. **Epic AC8 now satisfied — EPIC-001 has no open items left.** | @devops (Gage) |
