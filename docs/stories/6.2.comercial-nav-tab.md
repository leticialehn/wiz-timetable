# Story 6.2: "Comercial" nav tab + prospect list, split from Leads

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, typecheck, lint]

## Story

**As a** secretaria,
**I want** a dedicated, top-level "Comercial" tab listing every prospect booked
into a Comercial slot,
**so that** I can follow up on enrollment conversations without digging through the
general "Leads" report, which mixes them with experimental-class walk-ins and other
avulsos.

## Dependency

Depends on Story 6.1 (the `"comercial"` `TipoHorario` and its bookings existing at
all). Do not start before 6.1's Tasks 1-3 (app-level) land; this story's data layer
does not need 6.1's Task 4 (DB migration) to be _written_, but has nothing to
display until real Comercial bookings exist in production.

## Acceptance Criteria

1. A new top-level nav link "Comercial" appears in `admin.tsx`'s header nav
   (alongside Alunos/Relatórios/Calendário/Professoras/Alertas — **not** nested
   under Relatórios, per explicit product decision), routing to `/admin/comercial`.
2. `/admin/comercial` lists every prospect booked under `tipo === "comercial"`,
   grouped/deduplicated by name using the same normalization
   (`normalizarNomeParaComparacao`) the existing Leads report already uses, so
   "Joao"/"João" collapse to one entry the same way.
3. A new server function (e.g. `getProspectosComerciais`) mirrors `getLeads`'s
   query shape (`grade_base` + `excecoes_semana`, filtered on
   `aluno_nome_avulso is not null`) but adds `.eq("tipo", "comercial")` — do not
   filter client-side; keep the query narrow like `getLeads`'s existing
   `.not("aluno_nome_avulso", "is", null)` pattern.
4. `getLeads` (existing) is updated to **exclude** `tipo === "comercial"` rows
   (`.neq("tipo", "comercial")` alongside its existing filters), so a Comercial
   prospect appears on `/admin/comercial` only, never also on
   `/admin/relatorios/avulsos`.
5. The new page reuses `admin.relatorios_.avulsos.tsx`'s structure (search box,
   grouped list, "última aula" style "última conversa" or similar date) rather than
   inventing a new UI pattern — visually consistent with the rest of `/admin`.
6. Both `/admin/comercial` and the now-filtered `/admin/relatorios/avulsos` are
   reachable only to authenticated users, same as every other `/admin/*` route
   today (no new auth work needed — inherited from Stories 1.1/1.2).
7. `npm run typecheck` and `npm run lint` pass.

## 🤖 CodeRabbit Integration

**Enabled.** Primary type: Feature · Complexity: Low.
Focus: the `getLeads`/`getProspectosComerciais` split must be mutually exclusive
(no prospect counted in both, none silently dropped from both) — this is the one
thing a reviewer should verify by reading both queries side by side.

## Tasks / Subtasks

- [ ] Task 1: New server function (AC: 3)
  - [ ] `getProspectosComerciais` in `relatorios.functions.ts`, adapted from
        `getLeads`'s existing query + grouping logic (consider extracting the
        shared grouping/normalization helper instead of copy-pasting it twice).
- [ ] Task 2: Update `getLeads` (AC: 4)
  - [ ] Add the `tipo != "comercial"` exclusion to both its `grade_base` and
        `excecoes_semana` queries.
- [ ] Task 3: New route + nav link (AC: 1, 2, 5)
  - [ ] `src/routes/admin.comercial.tsx`, modeled on
        `admin.relatorios_.avulsos.tsx`.
  - [ ] Add the nav `<Link to="/admin/comercial">` in `admin.tsx`.
- [ ] Task 4: Verify (AC: 6, 7)
  - [ ] Confirm `/admin/comercial` inherits the same auth gate as other `/admin/*`
        routes (no per-route guard needed — verify by reading `__root.tsx`'s
        AuthGate wiring, not by assuming).
  - [ ] `npm run typecheck`, `npm run lint`.

## Dev Notes

- **Grounded 2026-09-14:** `getLeads` (`relatorios.functions.ts:261-336`) already
  has the exact grouping/normalization/variant-tracking logic this story needs —
  extracting it into a shared helper (`agruparAvulsosPorNome` or similar) taking
  the two query results and returning grouped `Lead[]`-shaped data avoids a second
  near-duplicate implementation. Prefer that over copy-pasting the function body.
- **Nav structure confirmed:** `admin.tsx` lines ~26-67 hold the header `<nav>`
  with five existing `<Link>`s in a flat row — the new one goes in the same list.
- `admin.relatorios_.avulsos.tsx` (the Leads page) is the direct UI template —
  it's ~80 lines, self-contained, easy to adapt.

### Testing

- No automated test harness yet (EPIC-003). Evidence = a manual check that a
  Comercial-tipo booking appears on `/admin/comercial` and does _not_ appear on
  `/admin/relatorios/avulsos`, and vice versa for a non-Comercial avulso (e.g. an
  "Exp" booking).

## Change Log

| Date       | Version | Description                                                                                                                                             | Author      |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2026-09-14 | 0.1     | Initial draft from a user feature request (session), grounded by reading `getLeads` and `admin.relatorios_.avulsos.tsx` in full, and `admin.tsx`'s nav. | @sm (River) |
