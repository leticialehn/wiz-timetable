# Story 3.2: GitHub Actions CI (typecheck + lint + test on push/PR)

## Status

Ready

## Executor Assignment

executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, typecheck, lint]

## Dependency

Depends on Story 3.1 for the `npm test` script to exist and be meaningful. Can be
drafted/reviewed in parallel with 3.1, but the workflow's `npm test` step has
nothing to run until 3.1 lands — sequence: land 3.1 first, or land this story's
`typecheck`+`lint` steps first and add the `test` step in the same PR as 3.1.

## Story

**As a** maintainer of this repo (currently a solo/small team, per CON-5),
**I want** GitHub Actions to run typecheck, lint, and tests on every push and PR to
`main`,
**so that** a broken change is visible as a failing check before it's treated as
mergeable, not just relying on local husky hooks that can be skipped or missing.

## Acceptance Criteria

1. A new workflow file `.github/workflows/ci.yml` runs on `push` to `main` and on
   `pull_request` targeting `main`.
2. The workflow runs, in order: `npm ci` (not `npm install` — lockfile-exact,
   reproducible), `npm run typecheck`, `npm run lint`, `npm test` (once Story 3.1
   exists; if this story lands first, add the `test` step in the same PR that adds
   3.1, not as a silent no-op).
3. A failing step fails the workflow run visibly (GitHub's standard check-run
   behavior — no need for custom status reporting).
4. No secrets are required — per Story 3.1's AC2, all three commands run without a
   Supabase connection. If a future story needs one (e.g. integration tests), that
   requires an explicit new secrets-management decision, not assumed here.
5. The workflow uses a pinned Node.js major version via `actions/setup-node`
   (project has no `.nvmrc`/`engines` field today — pick and document one, e.g.
   matching whatever LTS the team already develops against; exact version is an
   implementation choice, not prescribed here) plus `npm`'s cache
   (`actions/setup-node`'s `cache: 'npm'`) so runs aren't slower than necessary.
6. This CI check does **not** replace or duplicate the existing local husky
   pre-push hook (`typecheck && lint`, confirmed in `.husky/pre-push`) — CI is a
   second, server-side gate that can't be bypassed by a missing local hook
   install, not a replacement for the fast local feedback loop.
7. `docs/framework/tech-stack.md` (or wherever CI is documented) gets a short
   entry describing what CI checks and where to see results.

## 🤖 CodeRabbit Integration

**Enabled.** Primary type: Infra/CI-CD · Complexity: Low.
Focus: the workflow must not silently skip a step on failure (e.g. missing a
`continue-on-error`, or steps that swallow non-zero exit codes) — a red CI check
must actually mean something failed.

**Self-Healing Configuration**

- Primary Agent: @architect (matches this story's `quality_gate: "@architect"`)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL only

Predicted Behavior:

- CRITICAL issues: auto_fix (up to 2 iterations)
- HIGH issues: document_only (noted in Dev Notes/Completion Notes)

**Note:** CodeRabbit's CLI is not reachable in this dev environment (no Ubuntu WSL
distro) — substitute Claude Code's own `/code-review` + live verification, same
pattern used throughout EPIC-002 and EPIC-006.

## Tasks / Subtasks

- [ ] Task 1: Workflow file (AC: 1, 2, 3, 5)
  - [ ] `.github/workflows/ci.yml`: `on: push` (branches: [main]) +
        `pull_request` (branches: [main]); one job, `actions/checkout` +
        `actions/setup-node` (pinned version, `cache: 'npm'`) + `npm ci` +
        the three check steps in order.
- [ ] Task 2: Verify no secrets needed (AC: 4)
  - [ ] Confirm by reading each of the three commands' actual behavior (not
        assuming) that none reach out to Supabase/network in a way that would
        need credentials in CI.
- [ ] Task 3: Docs (AC: 7)
  - [ ] Short CI section in the stack docs: what runs, where to see results
        (Actions tab / PR checks).
- [ ] Task 4: Verify end-to-end (AC: 3, 6)
  - [ ] Push a throwaway branch with a deliberate lint error, confirm the
        workflow goes red; fix it, confirm green. Confirm the local
        `.husky/pre-push` hook is untouched by this story (`.husky/pre-push`
        file diff should be empty).

## Dev Notes

- **Grounded 2026-09-18**: confirmed no `.github/workflows/` directory exists yet
  (`ls` returns nothing); confirmed `.husky/pre-push`'s exact current content is
  `npm run typecheck && npm run lint` — this story adds `npm test` as a third
  check once 3.1 exists, both locally (optional, out of this story's scope to
  change husky) and in CI (in scope, AC2).
- **No `.nvmrc`/`package.json` `engines` field exists** — this story is a
  reasonable place to add one if the team wants Node version consistency between
  local dev and CI, but that's a nice-to-have, not a blocker; the workflow can
  simply pin a version in `actions/setup-node` without requiring a repo-wide
  `.nvmrc` addition.
- **This is @devops's story** (CI/CD pipeline management is @devops-exclusive per
  `.claude/rules/agent-authority.md`), unlike most of this epic's stories which
  are @dev.

### Testing

- This story's own test is the workflow itself succeeding/failing correctly (Task 4) — no unit tests apply to a YAML workflow file.

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                        | Author      |
| ---------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2026-09-18 | 0.1     | Initial draft, grounded by confirming no `.github/workflows/` exists and reading `.husky/pre-push`'s exact content. Part of EPIC-003, created same session as 3.1.                                                                                                                 | @sm (River) |
| 2026-09-22 | 0.2     | Validated GO (10/10) — re-confirmed `.husky/pre-push`'s content, absence of `.github/workflows/`, `npm` as the sole package manager (`package-lock.json` only), and the GitHub remote (`github.com/leticialehn/wiz-timetable`) — no drift, no issues found. Status: Draft → Ready. | @po (Pax)   |
