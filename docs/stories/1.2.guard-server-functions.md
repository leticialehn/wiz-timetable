# Story 1.2: Guard all data server functions with requireAuthenticated

## Status

InReview

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, typecheck, lint, endpoint-checklist]

## Story

**As a** data protection owner,
**I want** every server function that reads or writes school data to verify the
caller's session,
**so that** the RPC endpoints cannot be called directly to bypass the login screen.

## Acceptance Criteria

1. Every exported `createServerFn` in **all 8** data modules calls
   `await requireAuthenticated()` as the first statement of its handler (bare call is
   fine when the returned `usuario` is not needed):
   `alertas.functions.ts`, `cadastros.functions.ts`, `calendario.functions.ts`,
   `grade.functions.ts`, `historico.functions.ts`, `presenca.functions.ts`,
   `relatorios.functions.ts`, `usuarios.functions.ts`.
   Baseline count from `grep`: **39 endpoints** (see the pre-filled checklist in Dev
   Agent Record). If `grep` finds more at implementation time, guard those too and
   update the checklist.
2. `auth.functions.ts` is intentionally exempt: `login`, `logout`, `getSessaoAtual`
   stay public; `criarPrimeiroUsuario` keeps its existing "0 users" guard.
3. An unauthenticated call to any guarded endpoint fails with the existing
   `"Não autorizado. Faça login."` error and performs **no** DB read or write (guard
   is above the first `supabaseAdmin` / `sb()` call in every branch).
4. The checklist in Dev Agent Record is completed — every row `Done`, count matches
   the `grep` result.
5. The professora screens and admin screens still work end-to-end for a logged-in
   user (no over-guarding that breaks a legitimate flow).
6. `npm run typecheck` + `npm run lint` pass; pre-commit hook green.
7. **`usuarios.functions.ts` coordination:** after this story a logged-in `professor`
   could call `criarUsuario` / `removerUsuario` (auth-only, role check is Story 1.3).
   Either (a) ship 1.2 + 1.3 in the same PR, or (b) add
   `await requireRole(["secretaria"])` to the 5 `usuarios.functions.ts` endpoints in
   this story. Decide and record in Completion Notes; do not deploy 1.2 alone with
   `usuarios` endpoints only auth-gated.

## 🤖 CodeRabbit Integration

**Enabled.** Primary type: Security · Complexity: Medium.
Focus: every handler path guarded (including early-return branches), guard runs
**before** any `supabaseAdmin` call, no guard on the read-only `getSessaoAtual`.
Pre-Commit (@dev) required before Review.

## Tasks / Subtasks

- [x] Task 1: Inventory (AC: 4) — 39 endpoints across 8 data modules (@po pre-filled;
      re-grepped at `3ab68ea`, unchanged).
- [x] Task 2: Add the guard (AC: 1, 3)
  - [x] Guard applied **inside each file's DB-client helper**
        (`admin()` / `sb()` / `publicClient()`) rather than 39 copies of the line —
        every handler awaits its helper before any query, so this is a strictly
        stronger guarantee (a handler physically cannot reach the DB unauthenticated)
        and every handler's first DB-touching statement remains `await <helper>()`.
  - [x] Lazy `await import("./auth.server")` inside the helper (client-bundle-safe,
        matches the existing `client.server` import pattern).
  - [x] `grade.functions.ts` has two helpers (`admin` + `publicClient`) — both guarded.
- [x] Task 3: Keep auth endpoints public (AC: 2)
  - [x] `auth.functions.ts` helper left unguarded; added a comment explaining why
        (`login` / `logout` / `getSessaoAtual` must serve logged-out callers;
        `criarPrimeiroUsuario` self-guards on "0 users").
- [x] Task 3b: AC7 decision — chose **option (b)**: `usuarios.functions.ts` helper
      uses `requireRole(["secretaria"])` now (not just `requireAuthenticated`), so a
      logged-in professor cannot manage accounts before Story 1.3.
- [~] Task 4: Regression pass (AC: 5)
  - [x] `tsc --noEmit`, `eslint` (9 files), `npm run build` — all green; no
        `auth.server.ts` in the client bundle.
  - [~] Logged-in walkthrough + logged-out direct RPC call: **not runtime-verified**
    (no non-prod DB; dev-mode server-fn IDs differ from build, and the Story 1.1
    gate prevents the UI from ever issuing an unauthenticated RPC to observe).
    Deferred to @qa — see Completion Notes. The guard shares the exact code path
    as `getSessaoAtual`, which Story 1.1 proved works at runtime.

## Dev Notes

**Dependencies:** Story 1.1 (Done) — the session infra (`getSessaoAtual`, the
`escola-auth` cookie flow) is already wired. Story 1.3 (role checks) should follow
immediately or share this PR (see AC7).

- `requireAuthenticated()` (in `src/lib/auth.server.ts`) calls `usuarioDaSessao()`,
  which reads the `escola-auth` session cookie and loads the user + papéis via
  service role; throws `Error("Não autorizado. Faça login.")` when there is no valid
  session. Returns `UsuarioAutenticado` (`{id, nome, username, papeis, professora_id,
ativo}`).
- **Import boundary:** `*.functions.ts` bundle to the browser. `auth.server.ts` must be
  `await import()`-ed inside the handler, exactly like `client.server` already is
  (`docs/framework/coding-standards.md` → Imports).
- Do not add role checks here — that's Story 1.3. This story only proves identity.
- `presenca.functions.ts` handlers currently start straight at `const client = await
sb();` — the guard goes above that line.
- Server functions that return `{ ok: false, erro }` style: throwing from the guard is
  fine; the client already surfaces thrown errors via React Query.

### Testing

- No harness yet. Evidence = the endpoint checklist (all ✅) + the logged-out direct
  call test.
- When vitest lands (EPIC-003), add a test that imports each `*.functions.ts` module
  and asserts every export's handler rejects without a session (mock `usuarioDaSessao`).

## Change Log

| Date       | Version | Description                                                                                                                                                                                                     | Author      |
| ---------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2026-09-10 | 0.1     | Initial draft from brownfield discovery                                                                                                                                                                         | @sm (River) |
| 2026-09-10 | 0.2     | Validated GO (8/10) — Draft → Ready. Fixes: added missing `historico.functions.ts` to AC1 (Critical gap), endpoint count (39), pre-filled the 39-row checklist, AC7 for usuarios/1.3 coordination, Dependencies | @po (Pax)   |
| 2026-09-10 | 0.3     | Status: Ready → InProgress                                                                                                                                                                                      | @dev (Dex)  |
| 2026-09-10 | 0.4     | Implemented — guard in the 8 DB-client helpers (39 endpoints); usuarios → `requireRole(["secretaria"])` (AC7 opt b); auth.functions.ts comment. tsc/eslint/build green. Status: InProgress → InReview           | @dev (Dex)  |

## Dev Agent Record

### Endpoint checklist

Pre-filled by @po from `grep -rn "createServerFn" src/lib/*.functions.ts` at revision
`1bedfa6`. **39 endpoints across 8 files.** Dev: tick `Done` as each is guarded;
re-run the grep and add rows if anything changed.

| File                    | Export                    | Method | Guard                       | Done |
| ----------------------- | ------------------------- | ------ | --------------------------- | ---- |
| alertas.functions.ts    | getAlertasLicaoPendente   | GET    | requireAuthenticated        | ✅   |
| alertas.functions.ts    | getAniversariantesDoMes   | GET    | requireAuthenticated        | ✅   |
| alertas.functions.ts    | getAlertasAtivos          | GET    | requireAuthenticated        | ✅   |
| alertas.functions.ts    | resolverAlerta            | POST   | requireAuthenticated        | ✅   |
| alertas.functions.ts    | marcarContactado          | POST   | requireAuthenticated        | ✅   |
| cadastros.functions.ts  | criarProfessora           | POST   | requireAuthenticated        | ✅   |
| cadastros.functions.ts  | atualizarProfessora       | POST   | requireAuthenticated        | ✅   |
| cadastros.functions.ts  | removerProfessora         | POST   | requireAuthenticated        | ✅   |
| cadastros.functions.ts  | criarAluno                | POST   | requireAuthenticated        | ✅   |
| cadastros.functions.ts  | atualizarAluno            | POST   | requireAuthenticated        | ✅   |
| cadastros.functions.ts  | getUltimasLicoesPorAluno  | GET    | requireAuthenticated        | ✅   |
| cadastros.functions.ts  | removerAluno              | POST   | requireAuthenticated        | ✅   |
| calendario.functions.ts | getCalendarioExcecoes     | GET    | requireAuthenticated        | ✅   |
| calendario.functions.ts | criarCalendarioExcecoes   | POST   | requireAuthenticated        | ✅   |
| calendario.functions.ts | removerCalendarioExcecoes | POST   | requireAuthenticated        | ✅   |
| grade.functions.ts      | getGradeSemana            | GET    | requireAuthenticated        | ✅   |
| grade.functions.ts      | setHorarioConfig          | POST   | requireAuthenticated        | ✅   |
| grade.functions.ts      | removerHorarioConfig      | POST   | requireAuthenticated        | ✅   |
| grade.functions.ts      | adicionarAluno            | POST   | requireAuthenticated        | ✅   |
| grade.functions.ts      | removerCelula             | POST   | requireAuthenticated        | ✅   |
| grade.functions.ts      | alternarAusenciaAvisada   | POST   | requireAuthenticated        | ✅   |
| grade.functions.ts      | alternarVagaFechada       | POST   | requireAuthenticated        | ✅   |
| historico.functions.ts  | getHistoricoAluno         | GET    | requireAuthenticated        | ✅   |
| presenca.functions.ts   | setPresenca               | POST   | requireAuthenticated        | ✅   |
| presenca.functions.ts   | setNota                   | POST   | requireAuthenticated        | ✅   |
| presenca.functions.ts   | getLancamentosSemana      | GET    | requireAuthenticated        | ✅   |
| presenca.functions.ts   | setLicao                  | POST   | requireAuthenticated        | ✅   |
| presenca.functions.ts   | corrigirNivelHistorico    | POST   | requireAuthenticated        | ✅   |
| presenca.functions.ts   | getHistoricoLicoes        | GET    | requireAuthenticated        | ✅   |
| relatorios.functions.ts | getCargaProfessoras       | GET    | requireAuthenticated        | ✅   |
| relatorios.functions.ts | getFrequenciaAlunos       | GET    | requireAuthenticated        | ✅   |
| relatorios.functions.ts | getAniversariantes        | GET    | requireAuthenticated        | ✅   |
| relatorios.functions.ts | getLeads                  | GET    | requireAuthenticated        | ✅   |
| relatorios.functions.ts | getRelatorioMatriculas    | GET    | requireAuthenticated        | ✅   |
| usuarios.functions.ts   | listarUsuarios            | GET    | requireRole(["secretaria"]) | ✅   |
| usuarios.functions.ts   | criarUsuario              | POST   | requireRole(["secretaria"]) | ✅   |
| usuarios.functions.ts   | atualizarUsuario          | POST   | requireRole(["secretaria"]) | ✅   |
| usuarios.functions.ts   | redefinirSenha            | POST   | requireRole(["secretaria"]) | ✅   |
| usuarios.functions.ts   | removerUsuario            | POST   | requireRole(["secretaria"]) | ✅   |

### Agent Model Used

claude-sonnet-5 (@dev / Dex)

### Completion Notes List

- **Guard placement — deliberate deviation from AC1's literal "first statement of the
  handler":** the guard lives in each file's single DB-client helper (`admin()` /
  `sb()` / `publicClient()`), not repeated in 39 handlers. Rationale: every handler
  already `await`s its helper before touching Supabase, so this covers 100% of DB
  paths and makes it _impossible_ for a future handler in these files to query
  unauthenticated — stronger than per-handler, and no risk of missing one. AC3 (the
  actual security requirement — no DB read/write when unauthenticated) is fully met.
  Flag for @architect / @qa to confirm they're happy with helper-level enforcement.
- **8 helpers guarded:** alertas `publicClient`, cadastros `admin`, calendario
  `admin`, grade `admin` **and** `publicClient`, historico `publicClient`, presenca
  `sb`, relatorios `publicClient`, usuarios `admin`.
- **AC7 → option (b):** `usuarios.functions.ts` uses
  `await requireRole(["secretaria"])` in its `admin()` helper (all 5 endpoints), so a
  logged-in `professor`/`coordenador` gets "Não autorizado. Você não tem permissão"
  and cannot manage accounts. This pulls one bit of Story 1.3 forward; 1.3 still owns
  the rest (grade edits, cadastros, calendário role checks).
- **`auth.functions.ts` untouched** except a comment on its `admin()` helper stating
  why it stays unguarded.
- **Verification done:** `tsc --noEmit` ✅, `eslint` on all 9 changed files ✅,
  `npm run build` ✅ (client bundle does not include `auth.server.ts` — the lazy
  import boundary holds).
- **Verification NOT done (deferred to @qa):** runtime proof that (a) a logged-out
  RPC call is rejected with no DB access, and (b) a logged-in user's screens/reports
  still work (no over-guard regression). Blockers: no non-prod Supabase; dev-mode
  server-fn IDs differ from the build so raw curl can't hit them; and the Story 1.1
  gate means the UI never issues an unauthenticated RPC to observe. The guard shares
  the exact `usuarioDaSessao` → `getAuthSession` path that `getSessaoAtual` uses, and
  Story 1.1 proved that path works at runtime (SSR bootstrap form rendered). This is
  the same authenticated-walkthrough gap already logged as QA-1.1-02 — one walkthrough
  on a non-prod DB covers 1.1 + 1.2 + this story.
- **Regression risk assessment:** low. The guard only _adds_ a `requireAuthenticated`
  call at the top of the helper; a logged-in user (guaranteed present after Story 1.1)
  passes it and everything downstream is byte-identical to before.

### File List

**Modified (guard added to the DB-client helper):**

- `src/lib/alertas.functions.ts`
- `src/lib/cadastros.functions.ts`
- `src/lib/calendario.functions.ts`
- `src/lib/grade.functions.ts` (both `admin` and `publicClient`)
- `src/lib/historico.functions.ts`
- `src/lib/presenca.functions.ts`
- `src/lib/relatorios.functions.ts`
- `src/lib/usuarios.functions.ts` (`requireRole(["secretaria"])`)

**Modified (comment only):**

- `src/lib/auth.functions.ts`

## QA Results

_(qa fills in)_
