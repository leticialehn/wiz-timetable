# Story 4.1: Scope `useRealtimeGrade`'s invalidation to affected query keys

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, typecheck, lint]

## Story

**As a** user with the app open on any screen,
**I want** a Realtime change on one table to only refetch the queries that
actually depend on that table,
**so that** an unrelated edit elsewhere (e.g. someone changing the grade while I'm
looking at the aniversariantes report) doesn't refetch every active query on my
screen.

## Acceptance Criteria

1. `useRealtimeGrade` (`src/hooks/use-realtime-grade.ts`) no longer calls
   `qc.invalidateQueries()` with no arguments. Each subscribed table's
   `postgres_changes` handler invalidates only the query-key prefixes that
   consume that table's data.
2. The query-key → table mapping is derived from actually reading each of the 19
   current call sites (`grep -rln "useRealtimeGrade()" src/` matches 20 files —
   one is `src/hooks/use-realtime-grade.ts` itself, the definition, not a call
   site; exclude it, e.g. `grep -rln "useRealtimeGrade()" src/routes/`; re-count at
   implementation time, this grew from Story 2.2's 17 after Story 6.1/6.2 added
   `admin.comercial.tsx`), not guessed. Follow the scoped-invalidation pattern
   already used elsewhere in this codebase (`professora.tsx:925-926`:
   `qc.invalidateQueries({ queryKey: ["lancamentos-semana"] })` — React Query's
   prefix matching means this also matches `["lancamentos-semana", dataSegunda,
professoraId]`).
3. Every screen that currently receives a live update via `useRealtimeGrade`
   continues to receive one after the change — this is a scoping fix, not a
   feature removal. Spot-check at minimum: `admin.index.tsx` (grid editor) and
   `professora.tsx` (professora view) both still refresh live when the other
   changes the grade.
4. Tables not touched by a given event no longer cause unrelated queries (e.g.
   `["matriculas"]`, `["aniversariantes"]`, `["carga-professoras"]` on the
   relatórios pages) to refetch.
5. `npm run typecheck` and `npm run lint` pass. Which 5 tables are subscribed
   (`professoras`, `grade_base`, `horarios_config`, `excecoes_semana`,
   `aulas_licoes`) does **not** change — that's Story 2.2's decision, out of
   scope here.

## 🤖 CodeRabbit Integration

**Enabled.** Primary type: Refactor/Performance · Complexity: Medium.
Focus: no query key that should be invalidated is missed (silent live-update
regression is worse than the refetch-storm this story fixes) — the reviewer should
cross-check the final table→query-key mapping against a fresh grep of all 19+
call sites, not trust the story's own enumeration without re-verifying it's still
current.

**Self-Healing Configuration**

- Primary Agent: @architect (matches this story's `quality_gate: "@architect"`)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL only

Predicted Behavior:

- CRITICAL issues: auto_fix (up to 2 iterations)
- HIGH issues: document_only (noted in Dev Notes/Completion Notes)

**Note:** CodeRabbit's CLI is not reachable in this dev environment (no Ubuntu WSL
distro) — substitute Claude Code's own `/code-review` + live verification, same
pattern used throughout EPIC-002 and EPIC-006.

## Tasks / Subtasks

- [ ] Task 1: Enumerate query keys per table (AC: 2)
  - [ ] Re-grep all `useRealtimeGrade()` call sites and, for each, list its
        `useQuery` `queryKey`s and which of the 5 subscribed tables each one's
        data actually comes from (read the query's `queryFn`/server function,
        don't guess from the file name).
- [ ] Task 2: Rework `useRealtimeGrade` (AC: 1)
  - [ ] Build a `Record<(typeof TABELAS)[number], string[]>`-shaped map (table →
        array of query-key prefixes) from Task 1's findings.
  - [ ] Each table's `postgres_changes` handler calls
        `qc.invalidateQueries({ queryKey: [prefix] })` for each of its mapped
        prefixes, instead of the current blanket `qc.invalidateQueries()`.
- [ ] Task 3: Verify no regression (AC: 3, 4)
  - [ ] Manual check (or, if Story 3.1 has landed by now, a unit test against the
        mapping itself): grid edit on `admin.index.tsx` still live-updates
        `professora.tsx`, and vice versa; a report page (e.g.
        `admin.relatorios_.matriculas.tsx`) does **not** refetch on an unrelated
        grade edit.
  - [ ] `npm run typecheck`, `npm run lint`.

## Dev Notes

- **Grounded 2026-09-18**: confirmed live in `src/hooks/use-realtime-grade.ts`
  that every one of the 5 subscribed tables' `postgres_changes` handler calls
  the exact same `() => qc.invalidateQueries()` with zero arguments — there is
  no existing per-table differentiation to preserve, this is a from-scratch
  scoping.
- **19 call sites found** (`grep -rln "useRealtimeGrade()" src/routes/`, re-run
  2026-09-18 — the un-scoped `src/` version matches 20 files because it also
  matches the hook's own definition in `src/hooks/use-realtime-grade.ts`, not a
  call site; **found and fixed during PO validation**, the story's first draft
  miscounted this as 19 from the un-scoped grep, which actually returns 20).
  Grew from Story 2.2's count of 17 because Story 6.1 (comercial tipo) and 6.2
  (`admin.comercial.tsx`) both added new grade-dependent screens. This number
  will keep changing as the app grows — Task 1 must re-derive it at
  implementation time, not trust this story's count.
- **A scoped-invalidation pattern already exists** in this codebase
  (`professora.tsx:925-926`) — reuse that pattern/style rather than inventing a
  new one.
- **Not in scope**: changing which 5 tables are subscribed, adding new Realtime
  tables, or touching the high-sensitivity tables' "no Realtime" decision from
  Story 2.2 — this story only changes what happens _after_ an already-subscribed
  event fires.

### Testing

- No automated test harness existed when this epic was drafted; if Story 3.1
  (EPIC-003) has landed by the time this is implemented, a unit test against the
  table→query-key mapping itself (not requiring a live Supabase connection) is
  preferable to purely manual verification. Otherwise, manual spot-check per
  Task 3.

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                           | Author      |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2026-09-18 | 0.1     | Initial draft, grounded by reading `use-realtime-grade.ts` in full and re-grepping all current call sites (19, up from Story 2.2's 17). Part of EPIC-004, created same session.                                                                                                                                                                                                                                                       | @sm (River) |
| 2026-09-22 | 0.2     | Validated GO (9/10) — found and fixed a real off-by-one: the story's own `grep -rln "useRealtimeGrade()" src/` command matches 20 files (includes the hook's own definition in `use-realtime-grade.ts`), not 19 — clarified to exclude it (`src/routes/` scope) so the 19 call-site count is accurate. Scoped-invalidation precedent (`professora.tsx:925-926`) and query-key examples re-confirmed unchanged. Status: Draft → Ready. | @po (Pax)   |
