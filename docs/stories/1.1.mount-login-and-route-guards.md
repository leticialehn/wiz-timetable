# Story 1.1: Mount login and route guards

## Status

Draft

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, typecheck, lint, manual-walkthrough]

## Story

**As a** school staff member,
**I want** the app to require me to log in before I can see or change anything,
**so that** students' personal and academic data is not exposed to anyone with the URL.

## Acceptance Criteria

1. Visiting any route (`/`, `/professora*`, `/admin*`) while unauthenticated renders a
   login screen instead of the requested page.
2. The login screen uses the existing `LoginForm` component and the `login` server
   function; a wrong username/password shows the existing error message and does not
   navigate.
3. After a successful login the user is taken to the route they originally requested
   (or `/professora` if they hit `/`).
4. When there are 0 rows in `usuarios`, the login screen instead shows the
   first-user bootstrap form wired to `criarPrimeiroUsuario` (this state is reported by
   `getSessaoAtual().precisaBootstrap`).
5. A `logout` control is visible in both the admin header and the professora screen;
   it calls the `logout` server function and returns the user to the login screen.
6. `getSessaoAtual` is called once per navigation via a router `beforeLoad` (or a
   root-level loader) — not once per component.
7. `npm run typecheck` and `npm run lint` pass. The pre-commit hook passes without
   `--no-verify`.

## 🤖 CodeRabbit Integration

**Enabled** (`coderabbit_integration.enabled: true`, wsl mode).

- **Primary type:** Security · **Secondary:** Frontend · **Complexity:** Medium
- **Focus areas:** authentication flow, redirect-after-login correctness, no session
  data leaked to the client bundle, `beforeLoad` runs server-side only.
- **Pre-Commit (@dev):** run before marking Review. CRITICAL → auto-fix (2 iter);
  HIGH → auto-fix if iter < 2 else document.
- Report location: `docs/qa/coderabbit-reports/`.

## Tasks / Subtasks

- [ ] Task 1: Session context in the router (AC: 1, 6)
  - [ ] Add a `beforeLoad` on `__root` (or a shared `_authed` layout) that calls
        `getSessaoAtual` via `useServerFn` equivalent for loaders (`createServerFn`
        can be called directly server-side in `beforeLoad`).
  - [ ] Expose `{ autenticado, usuario, precisaBootstrap }` on the router context.
  - [ ] Decide guard placement: simplest is `__root` `beforeLoad` that, when
        `!autenticado`, renders the login screen component tree instead of `<Outlet/>`
        (no redirect needed). Keep `/` → `/professora` redirect for the authed case.
- [ ] Task 2: Login screen (AC: 2, 3, 4)
  - [ ] Create `src/routes/login.tsx` OR an inline `<AuthGate>` wrapper in `__root`.
  - [ ] Render `LoginForm` (authed=false) or the bootstrap form (`precisaBootstrap`).
  - [ ] On success, `router.invalidate()` + navigate to the pending location
        (`router.state.location` captured before showing the gate) or `/professora`.
- [ ] Task 3: Logout (AC: 5)
  - [ ] Add a logout button to `admin.tsx` header and the professora screen header.
  - [ ] `useMutation` → `logout` fn → `router.invalidate()` → land on the gate.
- [ ] Task 4: Verify no regression for the authed path (AC: 3, 7)
  - [ ] Manual walkthrough: bootstrap → login → /admin, /professora, a report, logout.
  - [ ] Record steps + results in Dev Agent Record.

## Dev Notes

**Relevant source (from `docs/architecture.md` §6 and the code):**

- `src/lib/auth.functions.ts` exports `login({username,senha})`,
  `logout()`, `getSessaoAtual()` → `{autenticado, usuario, precisaBootstrap}`,
  `criarPrimeiroUsuario({nome,username,senha})`. All are `createServerFn`.
- `src/lib/auth.server.ts` exports `usuarioDaSessao()`, `requireAuthenticated()`,
  `requireRole(papeis)`. Session cookie name `escola-auth`, 7 days, needs
  `SESSION_SECRET` env (Story 1.5 confirms it in Vercel).
- `src/components/LoginForm.tsx` exists and is currently imported by nobody.
- `src/routes/__root.tsx` provides the `QueryClient` context and the error/404
  boundaries; `head` sets the title. `src/routes/index.tsx` only does
  `throw redirect({ to: "/professora" })`.
- `src/routes/admin.tsx` renders the admin nav + `<Outlet/>` (no guard today).
- Router is created in `src/router.tsx` with `context: { queryClient }`.

**Design guidance:**

- Prefer the **render-the-gate-in-`__root`** approach over per-route redirects: one
  place to reason about, no redirect loops, preserves the requested URL naturally.
- `beforeLoad` runs on the server during SSR and on the client on navigation — calling
  a `createServerFn` there is fine. Do **not** call `usuarioDaSessao` directly from a
  route file (it's in `*.server.ts`, would break the client bundle) — go through
  `getSessaoAtual`.
- Keep the gate component minimal and unstyled-consistent with `__root`'s existing
  404/error components (centered card, `bg-background`, `text-foreground`).

**Out of scope:** guarding the server functions themselves — that's Story 1.2. This
story stops the UI from rendering; 1.2 stops direct RPC calls.

### Testing

No test harness yet (EPIC-003). For this story:

- Manual walkthrough is the acceptance evidence; record it in the Dev Agent Record.
- Do the bootstrap/login test against a **non-production** Supabase or with a test
  user you can delete — never lock yourself out of prod.
- Keep any new logic (e.g. "compute pending redirect target") as a small pure helper
  so it can be unit-tested once vitest lands.

## Change Log

| Date       | Version | Description                             | Author      |
| ---------- | ------- | --------------------------------------- | ----------- |
| 2026-09-10 | 0.1     | Initial draft from brownfield discovery | @sm (River) |

## Dev Agent Record

### Agent Model Used

_(dev fills in)_

### Debug Log References

_(dev fills in)_

### Completion Notes List

_(dev fills in)_

### File List

_(dev fills in)_

## QA Results

_(qa fills in)_
